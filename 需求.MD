项目需求文档 (PRD)：极客版终端个人 AI 秘书系统 (CLI AI Secretary)

1. 项目背景与愿景

在日常科研与生活中，个人面临严重的信息过载与碎片化管理痛点（如待办遗忘、财务账目繁杂、订阅扣费漏看、会议记录检索困难）。市面上的效率软件彼此孤立，且主流大模型产品存在幻觉问题，无法作为可靠的生产力工具。
本项目愿景：基于本地 Linux 环境与纯文本（Markdown）哲学，打造一个完全由自然语言驱动的、数据100%私有可控的个人数字秘书。它不仅是一个对话机器人，更是一个具备“工具调用、状态管理、长效记忆”的本地状态机。

2. 核心架构设计 (KISS 原则)

运行环境：Linux 终端 (CLI)，利用 Rich 库实现终端内富文本渲染。

主控逻辑：Python 3.11+ 配合自定义 ReAct 状态机（或 LangGraph），实现任务编排与大/小循环。

持久化存储：抛弃传统关系型数据库。结构化/半结构化状态全部存储于透明的 Markdown 文件中；非结构化文档利用本地 ChromaDB 构建向量索引。

底层执行：封装 Linux 原生命令（如 sed, grep, mkdir）作为 Agent 的 Tools。

3. 核心功能模块需求 (Functional Requirements)

3.1 动态纯文本追踪中枢 (Dynamic Markdown Ledger)

这是系统的核心状态记录模块，支持无限横向扩展。

【需求描述】：用户通过自然语言输入结构化碎片（如记账、写备忘、存密码、记优惠券），系统需自动进行格式化排版并物理写入。

【功能点】：

动态路由与建档：系统需具备“文件嗅探”能力。当用户提及新领域（如“健身记录”）时，自动创建 fitness.md 并初始化表头。

跨文件无缝追加 (Append)：精准识别意图，将“打车40元”写入 expenses.md 表格，将“明晚取快递”写入 tasks.md 复选框列表。

状态原地更新 (Update)：通过检索并定位，利用子进程调用 sed 等命令，将任务从 [ ] 变更为 [x]，实现状态流转。

3.2 极简 RAG 与文档库管理 (Knowledge Base)

用于处理长文本与非结构化知识的检索。

【需求描述】：能够上传会议记录、电子说明书等，并在对话时自动召回相关背景。

【功能点】：

本地向量化：支持 PDF/Markdown 文件的本地 Embedding（基于 ChromaDB），单库不分表。

元数据过滤 (Metadata)：入库时强制附加 doc_type (如 meeting, manual)、date 等标签，检索时支持多条件过滤。

溯源引用：回答问题时必须携带文档原始时间戳与名称（如：“根据2026年5月10日的架构评审会议...”）。

3.3 多源实用工具链 (Utility Tools)

【功能点】：

外挂天气 API：支持根据用户指定的城市和时间，精确查询天气状态。

标准扩展接口 (MCP/Skill)：系统预留标准化的 Tool 注册装饰器，允许未来快速接入联网搜索、发邮件等新能力。